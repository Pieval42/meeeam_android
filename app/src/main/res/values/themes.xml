<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Meeeam" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/meeeam_red</item>
        <item name="backgroundColor">@color/dark_background</item>
        <item name="strokeColor">@color/border_primary</item>
        <item name="colorSecondary">@color/text_warning</item>
        <item name="android:statusBarColor">@color/dark_background</item>
    </style>

    <style name="Theme.Meeeam" parent="Base.Theme.Meeeam" />
</resources>